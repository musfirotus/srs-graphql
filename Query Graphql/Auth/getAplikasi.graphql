# Request
query getAplikasi {
  getAplikasi {
    id,
    createdBy,
    updatedBy,
    createdAt,
    updatedAt,
    deletedAt,
    kodeAplikasi,
    nama,
    nama_singkat,
    icon,
    color
  }
}

# Headers
{
  "Authorization": "Bearer "
}